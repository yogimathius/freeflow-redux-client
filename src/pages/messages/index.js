import React from 'react';
import UserMessagesList from '../../features/messages/UserMessageList';

const index = () => {
    return (
        <div>
            <UserMessagesList></UserMessagesList>
        </div>
    );
};

export default index;